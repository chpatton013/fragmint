{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/yaml-frag/secrets.schema.json",
  "title": "yaml-frag secrets file",
  "description": "Structural schema for the optional, untracked secrets file passed via --secrets. A flat name -> value store referenced by `from: secret` variable sources. See README.md 'Secrets'. Values must never be logged.",
  "type": "object",
  "additionalProperties": false,
  "required": ["secrets"],
  "properties": {
    "secrets": {
      "type": "object",
      "description": "Mapping of secret name to value.",
      "additionalProperties": true
    }
  }
}
