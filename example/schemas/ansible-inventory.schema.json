{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Ansible inventory (example aggregate output)",
  "description": "Whole-document validation for the ansible-inventory output. See README.md 'Aggregate outputs': an assert inside an aggregate fragment only ever sees the partial document built so far, so this schema (not a trailing assertion fragment) is what checks the FINISHED, fully-composed document.",
  "type": "object",
  "required": ["all"],
  "properties": {
    "all": {
      "type": "object",
      "required": ["children"],
      "properties": {
        "children": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "required": ["hosts"],
            "properties": {
              "hosts": {
                "type": "object",
                "additionalProperties": {
                  "type": "object",
                  "required": ["ansible_host"],
                  "properties": {
                    "ansible_host": { "type": "string" }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
