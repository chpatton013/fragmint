fragment:
  version: 1
  name: roles/proxmox-host
  description: Bootstrap prerequisites for a future Proxmox host.

# Placeholder until the exact Proxmox installation flow is decided. It still
# demonstrates explicit composition (see README.md "Authoring fragments").
operations:
  - op: append
    path: /autoinstall/user-data/packages
    deduplicate: true
    value:
      - curl
      - gnupg
      - ca-certificates
