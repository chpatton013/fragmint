fragment:
  version: 1
  name: ubuntu-24.04
  description: Ubuntu 24.04 Server installation source.

# Do not place the HWE kernel here; it may be hardware-specific. See PLAN.md.
operations:
  - op: set
    path: /autoinstall/source
    value:
      id: ubuntu-server-minimal
      search_drivers: false
