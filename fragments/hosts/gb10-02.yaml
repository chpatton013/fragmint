fragment:
  version: 1
  name: hosts/gb10-02
  description: Host-specific configuration for gb10-02.

# See hosts/gb10-01. gb10-02 should differ from gb10-01 only in host-specific
# inventory variables unless it explicitly selects different fragments
# (PLAN.md "Snapshot tests").
operations:
  - op: assert
    path: /autoinstall/storage/layout/name
    equals: lvm
